//import getCurrentUser from '@/app/actions/getCurrentUser';
'use client'

import { useEffect } from 'react'

async function Providerbutton({ children }: { children: React.ReactNode }) {
  useEffect(() => {})

  return <div>{children}</div>
}

export default Providerbutton
